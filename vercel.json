{
  "buildCommand": "cd mobile-pwa && npm install && npm run build && cd ../prep-app && npm install && npm run build",
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    {
      "source": "/prep/((?!assets/).*)",
      "destination": "/prep/index.html"
    }
  ]
}
