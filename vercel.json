{
  "buildCommand": "npm install && cd mobile-pwa && npm run build && cd ../desktop && npm run build:islands",
  "outputDirectory": ".",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/mobile/(.*)", "dest": "/mobile/$1" }
  ]
}
