{
  "buildCommand": "cd mobile-pwa && npm install && npm run build && cd ../desktop && npm install && npm run build && cd .. && rm -rf desktop && mv desktop-out desktop",
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    { "source": "/desktop", "destination": "/desktop/index.html" },
    { "source": "/desktop/(.*)", "destination": "/desktop/index.html" }
  ]
}
